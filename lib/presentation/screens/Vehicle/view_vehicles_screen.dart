import 'package:flutter/material.dart';

class ViewVehicleScreen extends StatelessWidget {
  const ViewVehicleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}